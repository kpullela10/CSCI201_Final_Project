# Backend Environment Variables
# These are typically set in Railway dashboard, not in a local .env file
# This file documents what environment variables are needed

# Database Configuration
DATABASE_URL=jdbc:mysql://mysql.railway.internal:3306/railway?useSSL=false&serverTimezone=UTC
DATABASE_USERNAME=root
DATABASE_PASSWORD=your-database-password-here

# JWT Configuration
# Generate a secure random string (minimum 256 bits)
JWT_SECRET=your-very-long-secret-key-change-this-in-production-minimum-256-bits

# Image Storage Path (optional, defaults to ./uploads/)
IMAGE_STORAGE_PATH=./uploads/
