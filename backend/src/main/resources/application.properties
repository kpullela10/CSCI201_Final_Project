# Server Configuration
server.port=8080

# MySQL Database Configuration
# Using Railway MySQL - Public connection for local testing
spring.datasource.url=${DATABASE_URL:jdbc:mysql://trolley.proxy.rlwy.net:45740/railway?useSSL=true&serverTimezone=UTC}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:NTTfVCZPjPkTxPFwVaXTKbDRkrFbKqRO}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=${JWT_SECRET:your-very-long-secret-key-change-this-in-production-minimum-256-bits}
jwt.expiration=86400000

# File Upload Configuration (for Pin/Maps team)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true

# Image Storage Path (for Pin/Maps team)
image.storage.path=${IMAGE_STORAGE_PATH:./uploads/}

# Logging
logging.level.com.usc.squirrelspotter=DEBUG
logging.level.org.springframework.security=DEBUG
